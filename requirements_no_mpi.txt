# requirements for the scan package but without any MPI dependencies for systems that don't have it installed.

numpy >=1.14.5, <= 2.1.3

networkx >= 2.6.0, <= 3.4.2
pandas >=1.0.0, <= 2.2.3

scipy >=1.4.0, <= 1.14.1
scikit-learn >= 0.20.0, <= 1.5.2

loguru >= 0.4.0, <= 0.7.2
