# requirement.txt for scan testing. Specifies the newest package versions we support.

numpy <= 1.22.3

networkx !=2.7, <= 2.8.3 # networkx 2.7 is broken on python 3.6 and maybe other versions of python too
pandas <= 1.4.2

scipy <= 1.8.1
scikit-learn <= 1.0.2

loguru <= 0.5.3

mpi4py <= 3.1.3
